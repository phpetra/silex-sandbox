{% extends "layout.html.twig" %}

{% block content %}
    <div class="row">
        <div class="col-md-12">

            <h1>Details of {{ sample.title }}</h1>

            <div class="row">
                <div class="col-md-6">

                    <p><i class="fa fa-calendar"></i>{{ sample.created_on|date('d-m-Y') }}</p>

                    <p>{{ sample.description }}</p>

                    <p><a href="{{ path('sample-all') }}">Back to list</a> </p>
                </div>

                <div class="col-md-6">



                </div>
            </div>

        </div>
    </div>


{% endblock %}
